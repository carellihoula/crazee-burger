// eslint-disable-next-line react/prop-types
export const Logo = (props) =>{
    
    return(
        <>
                <img src={props.imageUrl} width={props.longueur} height={props.hauteur}/>
        </>
        
    )
}
